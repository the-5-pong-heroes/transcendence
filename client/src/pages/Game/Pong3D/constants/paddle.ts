import { GAME_HEIGHT } from "./game";

export const PADDLE_HEIGHT_RATIO = 0.12;
export const PADDLE_WIDTH_RATIO = 0.02;
export const PADDLE_DEPTH_RATIO = 0.24;
export const PADDLE_WIDTH_PX = 10;
export const PADDLE_OFFSET_PX = 20;

export const PADDLE_VELOCITY = GAME_HEIGHT / 2;
